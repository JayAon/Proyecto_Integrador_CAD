export interface Prediction {
  maquina: string;
  seccion: string;
  proceso: string;
  usuario: string;
  fabricadas: number;
  turno: string;
  referencia: string;
  duracion: number;
  optimal: boolean;
}
